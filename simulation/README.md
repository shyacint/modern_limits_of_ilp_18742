* The "Results" folder contains the results of the experiments which were discussed in the final report. 
    * exp2 is the single instruction window.
    * exp3-5 are the double instruction windows, with 1x, 2x, and 3x gap multipliers, respectively. 
* The "FinalVersion" folder contains the code for the script which was used to produce these results.
* The "ProfileVersion" is a modified version of this script which we used to profile the traces based on ALU/Memory/Branch operation. 